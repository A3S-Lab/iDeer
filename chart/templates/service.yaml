apiVersion: v1
kind: Service
metadata:
  name: {{ include "deep-research.fullname" . }}
  labels:
    {{- include "deep-research.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deep-research.selectorLabels" . | nindent 4 }}
